// Counting Tilings 
#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(false), cin.tie(0), cout.tie(0)
using namespace std;
#define lli long long int

int main()
{
    fastio;
    const int mxN = 10, mxM = 1000, M = 1e9+7;
    int n, m;
    cin >> n >> m;
    lli ans = 0;

    cout << ans << "\n";
    return 0;
}
