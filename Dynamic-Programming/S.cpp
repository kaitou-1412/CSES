// Counting Numbers 
#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(false), cin.tie(0), cout.tie(0)
using namespace std;
#define lli long long int

int main()
{
    fastio;
    lli a, b;
    cin >> a >> b;
    lli ans = 0;

    cout << ans << "\n";
    return 0;
}
